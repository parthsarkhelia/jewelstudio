services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ijewel
      POSTGRES_PASSWORD: ijewel_dev
      POSTGRES_DB: ijewel
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ijewel"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:latest
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: ijewel
      MINIO_ROOT_PASSWORD: ijewel_dev
    volumes:
      - miniodata:/data
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 5s
      retries: 5

  minio-setup:
    image: minio/mc:latest
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      mc alias set local http://minio:9000 ijewel ijewel_dev;
      mc mb --ignore-existing local/ijewel-models;
      mc mb --ignore-existing local/ijewel-thumbnails;
      mc mb --ignore-existing local/ijewel-exports;
      mc anonymous set download local/ijewel-thumbnails;
      exit 0;
      "

volumes:
  pgdata:
  miniodata:
